<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:sec="http://www.springframework.org/schema/security"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:b="http://www.springframework.org/schema/mvc"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
         http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
         http://www.springframework.org/schema/security
         http://www.springframework.org/schema/security/spring-security.xsd">

    <!-- form-login に認証成功ハンドラーを指定 -->
    <bean id="customAuthenticationSuccessHandler"
          class="com.mvc.web.security.handler.CustomAuthenticationSuccessHandler"/>
    <!-- form-login に認証失敗ハンドラーを指定 -->
    <bean id="customAuthenticationFailureHandler"
          class="com.mvc.web.security.handler.CustomAuthenticationFailureHandler"/>

    <!-- ログイン画面のcssファイルとしても共通のdemo.cssを利用するため、 -->
    <!-- src/main/webapp/resources/static/bootstrapフォルダ下は常にアクセス可能とする -->
    <!--    <sec:http pattern="/resources/static/bootstrap/**" security="none"/>-->

    <!-- H2コンソールは認証不要 -->
    <sec:http pattern="/h2-console/**" security="none"/>

    <sec:http auto-config="true" use-expressions="true">
        <!-- フレームの許可（デフォルトではすべて deny） -->
        <sec:headers>
            <!-- 自分自身のアプリ内で iframe 表示が可能 -->
            <sec:frame-options policy="SAMEORIGIN"/>
        </sec:headers>
        <!-- ログイン画面は常にアクセス可能とする -->
        <sec:intercept-url pattern="/login" access="permitAll"/>
        <!-- ログアウト画面は常にアクセス可能とする -->
        <sec:intercept-url pattern="/logout" access="permitAll"/>
        <!-- 静的リソースは認証不要 -->
        <sec:intercept-url pattern="/static/**" access="permitAll"/>
        <!-- それ以外の画面は全て認証を有効にする -->
        <sec:intercept-url pattern="/**" access="isAuthenticated()"/>
        <!-- ログインに成功したら検索画面に遷移する -->
        <sec:form-login login-page="/login"
                        authentication-success-handler-ref="customAuthenticationSuccessHandler"
                        authentication-failure-handler-ref="customAuthenticationFailureHandler"
                        username-parameter="username"
                        password-parameter="password"
        />
        <!-- ログアウト時はログイン画面に遷移する -->
        <sec:logout logout-url="/logout"
                    logout-success-url="/login"
                    invalidate-session="true"
                    delete-cookies="JSESSIONID"
        />
        <!-- CSRF保護 -->
        <sec:csrf/>
    </sec:http>

    <!-- ユーザー定義 -->
    <sec:authentication-manager>
        <sec:authentication-provider>
            <sec:user-service>
                <!-- ユーザー名「user」、パスワード「password」が入力されたらログイン可能とする -->
                <sec:user name="user" password="{noop}password" authorities="ROLE_USER"/>
            </sec:user-service>
        </sec:authentication-provider>
    </sec:authentication-manager>
</beans>