<?xml version="1.0" encoding="UTF-8"?>
<web-app version="4.0"
         xmlns="http://xmlns.jcp.org/xml/ns/javaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="
           http://xmlns.jcp.org/xml/ns/javaee
           http://xmlns.jcp.org/xml/ns/javaee/web-app_4_0.xsd">

    <!--    <display-name>spring web</display-name>-->

    <!-- ビジネスロジックのBean 定義ファイル -->
    <!-- Spring Security用の定義ファイル securityContext.xmlを読み込み対象に追加 -->
    <context-param>
        <param-name>contextConfigLocation</param-name>
        <param-value>
            classpath:/spring/spring.xml
            classpath:/spring/springMvcContext.xml
            classpath:/spring/securityContext.xml
            classpath:/spring/applicationContext.xml
        </param-value>
    </context-param>

    <!-- リスナーを登録 -->
    <listener>
        <listener-class>org.springframework.web.context.ContextLoaderListener</listener-class>
    </listener>

    <!-- Spring MVC アプリの場合、だいたいは唯一のサーブレットを登録 -->
    <servlet>
        <servlet-name>dispatcherServlet</servlet-name>
        <servlet-class>org.springframework.web.servlet.DispatcherServlet</servlet-class>
        <!-- Spring MVC の Bean 定義を登録 -->
        <init-param>
            <param-name>contextConfigLocation</param-name>
            <!--            <param-value>classpath:/spring/springMvcContext.xml</param-value>-->
            <param-value/>
        </init-param>
        <load-on-startup>1</load-on-startup>
    </servlet>
    <!-- H2コンソール用Servlet -->
    <servlet>
        <servlet-name>h2-console</servlet-name>
        <servlet-class>org.h2.server.web.WebServlet</servlet-class>
        <init-param>
            <param-name>webAllowOthers</param-name>
            <param-value>true</param-value>
        </init-param>
    </servlet>

    <!-- すべての URL リクエストについて、上記で登録したサーブレットで処理 -->
    <servlet-mapping>
        <servlet-name>dispatcherServlet</servlet-name>
        <url-pattern>/</url-pattern>
    </servlet-mapping>
    <!-- H2コンソール用Servlet Mapping -->
    <servlet-mapping>
        <servlet-name>h2-console</servlet-name>
        <url-pattern>/h2-console/*</url-pattern>
    </servlet-mapping>

    <!-- リクエストパラメータのエンコーディング指定 -->
    <filter>
        <filter-name>CharacterEncodingFilter</filter-name>
        <filter-class>org.springframework.web.filter.CharacterEncodingFilter</filter-class>
        <init-param>
            <param-name>encoding</param-name>
            <param-value>UTF-8</param-value>
        </init-param>
        <init-param>
            <param-name>forceEncoding</param-name>
            <param-value>true</param-value>
        </init-param>
    </filter>
    <!-- Spring Security Filter -->
    <filter>
        <filter-name>springSecurityFilterChain</filter-name>
        <filter-class>org.springframework.web.filter.DelegatingFilterProxy</filter-class>
    </filter>

    <!-- 上記フィルターをすべての URL で適用 -->
    <filter-mapping>
        <filter-name>CharacterEncodingFilter</filter-name>
        <url-pattern>/*</url-pattern>
    </filter-mapping>
    <filter-mapping>
        <filter-name>springSecurityFilterChain</filter-name>
        <url-pattern>/*</url-pattern>
    </filter-mapping>
</web-app>
